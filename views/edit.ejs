<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Note - Notes App</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <nav>
      <h1>Notes App</h1>
      <div class="nav-links">
        <a href="/notes">Back to Notes</a>
        <a href="/auth/logout">Logout</a>
      </div>
    </nav>
  </header>
  <main>
    <div class="add-note-section">
      <h2>Edit Note</h2>
      <form method="post" action="/notes/edit/<%= note._id %>" class="note-form">
        <input type="text" name="title" value="<%= note.title %>" class="note-input" required>
        <textarea name="content" class="note-textarea" required><%= note.content %></textarea>
        <button type="submit" class="btn btn-primary">Update Note</button>
      </form>
    </div>
  </main>
</body>
</html>